import { useState, useCallback } from 'react'
import {
  Drawer,
  Group,
  UnstyledButton,
  Avatar,
  Text,
  Autocomplete,
} from '@mantine/core'
const DelegateDrawer = ({ opened, setOpened }) => {
  const [opened, setOpened] = useState(false)
  return null
}

export default DelegateDrawer
